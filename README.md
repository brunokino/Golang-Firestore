Just a lab using Golang to interact with Firestore (Firebase NoSQL Database )

The files ".env" and "credentials.json" are required to run

You can get the "credentials.json" at Firebase admin console